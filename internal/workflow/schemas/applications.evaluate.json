{
  "_generated": true,
  "credential_platform": null,
  "fields": [
    {
      "help": "The job application to score.",
      "key": "application_id",
      "label": "Application ID",
      "required": true,
      "type": "text"
    },
    {
      "default": "claude",
      "help": "Local agent runtime to use (e.g. claude), or jev / jev:<model> to score with TypeSafe Jev: one request answers the gates and rubric dimensions and the verdict is computed with the same weights and bands. Needs the vault secret typesafe or TYPESAFE_API_KEY (no fallback), sends the job posting and profile excerpts to TypeSafe.",
      "key": "runtime",
      "label": "Runtime",
      "required": false,
      "type": "text"
    },
    {
      "default": "default",
      "help": "Which profile owns this application.",
      "key": "profile_id",
      "label": "Profile ID",
      "required": false,
      "type": "text"
    }
  ]
}
