{
  "_generated": true,
  "credential_platform": null,
  "fields": [
    {
      "default": "",
      "help": "Force a specific platform. Leave blank to use the platform field from each input item.",
      "key": "platform",
      "label": "Platform Override",
      "options": [
        "",
        "linkedin",
        "instagram",
        "x",
        "tiktok"
      ],
      "required": false,
      "type": "select"
    },
    {
      "default": "default",
      "help": "Which profile owns the saved people. Defaults to 'default' — set this explicitly if this workflow belongs to a different profile (find the ID via 'monoagentcli profile list').",
      "key": "profile_id",
      "label": "Profile ID",
      "required": false,
      "type": "text"
    }
  ]
}
