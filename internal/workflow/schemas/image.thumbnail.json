{
  "credential_platform": null,
  "fields": [
    { "key": "field", "label": "Image Path Field", "type": "text", "required": false, "help": "Item field holding the local image path. Defaults to image_path, path, file_path, or media_path (first non-empty)." },
    { "key": "width", "label": "Width (px)", "type": "number", "required": true, "help": "Exact output width." },
    { "key": "height", "label": "Height (px)", "type": "number", "required": true, "help": "Exact output height." },
    { "key": "anchor", "label": "Anchor", "type": "select", "required": false, "options": ["center", "top", "bottom", "left", "right"], "default": "center", "help": "Which part of the image to keep when center-cropping to the exact dimensions." },
    { "key": "output_field", "label": "Output Field", "type": "text", "required": false, "default": "image_path", "help": "Item field where the output file path is written." },
    { "key": "output_dir", "label": "Output Directory", "type": "text", "required": false, "help": "Directory for the thumbnail file. Defaults to the source image's directory. ~ is expanded." }
  ]
}
