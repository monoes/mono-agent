{
  "_generated": true,
  "credential_platform": null,
  "fields": [
    {
      "help": "Item field holding the local image path. Defaults to image_path, path, file_path, or media_path (first non-empty).",
      "key": "field",
      "label": "Image Path Field",
      "required": false,
      "type": "text"
    },
    {
      "help": "Exact output width.",
      "key": "width",
      "label": "Width (px)",
      "required": true,
      "type": "number"
    },
    {
      "help": "Exact output height.",
      "key": "height",
      "label": "Height (px)",
      "required": true,
      "type": "number"
    },
    {
      "default": "center",
      "help": "Which part of the image to keep when center-cropping to the exact dimensions.",
      "key": "anchor",
      "label": "Anchor",
      "options": [
        "center",
        "top",
        "bottom",
        "left",
        "right"
      ],
      "required": false,
      "type": "select"
    },
    {
      "default": "image_path",
      "help": "Item field where the output file path is written.",
      "key": "output_field",
      "label": "Output Field",
      "required": false,
      "type": "text"
    },
    {
      "help": "Directory for the thumbnail file. Defaults to the source image's directory. ~ is expanded.",
      "key": "output_dir",
      "label": "Output Directory",
      "required": false,
      "type": "text"
    }
  ]
}
