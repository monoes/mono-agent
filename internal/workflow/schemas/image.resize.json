{
  "_generated": true,
  "credential_platform": null,
  "fields": [
    {
      "help": "Item field holding the local image path. Defaults to image_path, path, file_path, or media_path (first non-empty).",
      "key": "field",
      "label": "Image Path Field",
      "required": false,
      "type": "text"
    },
    {
      "help": "Target width. At least one of width or height is required.",
      "key": "width",
      "label": "Width (px)",
      "required": false,
      "type": "number"
    },
    {
      "help": "Target height. At least one of width or height is required.",
      "key": "height",
      "label": "Height (px)",
      "required": false,
      "type": "number"
    },
    {
      "default": "contain",
      "help": "contain: fit within bounds keeping aspect ratio； cover: fill and center-crop； fill: stretch to exact size； width/height: set only that dimension.",
      "key": "fit",
      "label": "Fit Mode",
      "options": [
        "contain",
        "cover",
        "fill",
        "width",
        "height"
      ],
      "required": false,
      "type": "select"
    },
    {
      "default": "image_path",
      "help": "Item field where the output file path is written.",
      "key": "output_field",
      "label": "Output Field",
      "required": false,
      "type": "text"
    },
    {
      "help": "Directory for the resized file. Defaults to the source image's directory. ~ is expanded.",
      "key": "output_dir",
      "label": "Output Directory",
      "required": false,
      "type": "text"
    }
  ]
}
