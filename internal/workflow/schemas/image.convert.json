{
  "_generated": true,
  "credential_platform": null,
  "fields": [
    {
      "help": "Item field holding the local image path. Defaults to image_path, path, file_path, or media_path (first non-empty).",
      "key": "field",
      "label": "Image Path Field",
      "required": false,
      "type": "text"
    },
    {
      "help": "Output image format. jpg is accepted as an alias for jpeg； webp can be decoded but not written.",
      "key": "format",
      "label": "Target Format",
      "options": [
        "jpeg",
        "png",
        "gif",
        "tiff",
        "bmp"
      ],
      "required": true,
      "type": "select"
    },
    {
      "default": 85,
      "help": "JPEG encode quality (only used when format is jpeg).",
      "key": "quality",
      "label": "JPEG Quality",
      "max": 100,
      "min": 1,
      "required": false,
      "type": "number"
    },
    {
      "default": "image_path",
      "help": "Item field where the output file path is written.",
      "key": "output_field",
      "label": "Output Field",
      "required": false,
      "type": "text"
    },
    {
      "help": "Directory for the converted file. Defaults to the source image's directory. ~ is expanded.",
      "key": "output_dir",
      "label": "Output Directory",
      "required": false,
      "type": "text"
    }
  ]
}
