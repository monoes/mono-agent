{
  "_generated": true,
  "credential_platform": null,
  "fields": [
    {
      "help": "Name of the vault entry to decrypt (as shown in the Vault page / monoagentcli secret list).",
      "key": "name",
      "label": "Name",
      "required": true,
      "type": "text"
    },
    {
      "default": "credential",
      "help": "Item field the decrypted fields object is written to. WARNING: this puts the plaintext secret into the item stream — it is stored unmasked in execution history and flows to every downstream node.",
      "key": "output_field",
      "label": "Output Field",
      "required": false,
      "type": "text"
    }
  ]
}
