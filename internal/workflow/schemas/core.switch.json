{
  "_generated": true,
  "credential_platform": null,
  "fields": [
    {
      "help": "Value expression to match against cases.",
      "key": "field",
      "label": "Field to Switch On",
      "placeholder": "{{ $json.status }}",
      "required": true,
      "type": "text"
    },
    {
      "help": "Each value becomes an output handle. Type a value and press Enter.",
      "item_type": "text",
      "key": "cases",
      "label": "Cases",
      "required": true,
      "type": "array"
    },
    {
      "default": "default",
      "help": "Output handle name for items that don't match any case.",
      "key": "default_handle",
      "label": "Default Handle",
      "required": false,
      "type": "text"
    },
    {
      "default": false,
      "help": "If enabled, an item can match more than one case instead of stopping at the first match.",
      "key": "fallthrough",
      "label": "Fallthrough to 'default'",
      "required": false,
      "type": "boolean"
    }
  ]
}
