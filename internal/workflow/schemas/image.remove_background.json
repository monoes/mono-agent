{
  "credential_platform": null,
  "fields": [
    { "key": "field", "label": "Image Path Field", "type": "text", "required": false, "help": "Item field holding the local image path. Defaults to image_path, path, file_path, or media_path (first non-empty)." },
    { "key": "bgcolor", "label": "Background Color", "type": "text", "required": false, "placeholder": "e.g. #ffffff or fff", "help": "Flatten the transparent background onto this hex color instead of keeping alpha. Output is always PNG." },
    { "key": "output_field", "label": "Output Field", "type": "text", "required": false, "default": "image_path", "help": "Item field where the output file path is written." },
    { "key": "output_dir", "label": "Output Directory", "type": "text", "required": false, "help": "Directory for the output PNG. Defaults to the source image's directory. ~ is expanded." }
  ]
}
