{
  "_generated": true,
  "credential_platform": null,
  "fields": [
    {
      "help": "Item field holding the local image path. Defaults to image_path, path, file_path, or media_path (first non-empty).",
      "key": "field",
      "label": "Image Path Field",
      "required": false,
      "type": "text"
    },
    {
      "help": "Flatten the transparent background onto this hex color instead of keeping alpha. Output is always PNG.",
      "key": "bgcolor",
      "label": "Background Color",
      "placeholder": "e.g. #ffffff or fff",
      "required": false,
      "type": "text"
    },
    {
      "default": "image_path",
      "help": "Item field where the output file path is written.",
      "key": "output_field",
      "label": "Output Field",
      "required": false,
      "type": "text"
    },
    {
      "help": "Directory for the output PNG. Defaults to the source image's directory. ~ is expanded.",
      "key": "output_dir",
      "label": "Output Directory",
      "required": false,
      "type": "text"
    }
  ]
}
