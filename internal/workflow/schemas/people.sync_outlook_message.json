{
  "_generated": true,
  "credential_platform": null,
  "fields": [
    {
      "default": "outlook",
      "help": "Stored as the message's source, e.g. outlook, gmail",
      "key": "source",
      "label": "Source Label",
      "required": false,
      "type": "text"
    },
    {
      "default": "inbound",
      "help": "inbound: sync each message's sender as the person (use with an inbox read). outbound: sync each recipient as the person instead (use with a sentitems/sent-mail read).",
      "key": "direction",
      "label": "Direction",
      "options": [
        "inbound",
        "outbound"
      ],
      "required": false,
      "type": "select"
    },
    {
      "default": "default",
      "help": "Which profile owns the synced people/messages. Defaults to 'default' — set this explicitly if this workflow belongs to a different profile (find the ID via 'monoagentcli profile list').",
      "key": "profile_id",
      "label": "Profile ID",
      "required": false,
      "type": "text"
    }
  ]
}
