{
  "_generated": true,
  "credential_platform": null,
  "fields": [
    {
      "help": "URL path suffix. Required — activation fails if left blank.",
      "key": "path",
      "label": "Webhook Path",
      "placeholder": "/webhook/my-hook",
      "required": true,
      "type": "text"
    },
    {
      "default": "POST",
      "key": "method",
      "label": "HTTP Method",
      "options": [
        "GET",
        "POST",
        "PUT",
        "PATCH",
        "DELETE"
      ],
      "required": false,
      "type": "select"
    },
    {
      "help": "If set, incoming requests must include a valid HMAC signature computed with this secret.",
      "key": "hmac_secret",
      "label": "HMAC Secret",
      "required": false,
      "type": "password"
    },
    {
      "help": "If set, requests must include this header with the auth token value.",
      "key": "auth_header",
      "label": "Auth Header Name",
      "placeholder": "X-Webhook-Secret",
      "required": false,
      "type": "text"
    },
    {
      "help": "Secret value expected in the auth header.",
      "key": "auth_token",
      "label": "Auth Token",
      "required": false,
      "type": "password"
    }
  ]
}
