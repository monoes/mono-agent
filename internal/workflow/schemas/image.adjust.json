{
  "credential_platform": null,
  "fields": [
    { "key": "field", "label": "Image Path Field", "type": "text", "required": false, "help": "Item field holding the local image path. Defaults to image_path, path, file_path, or media_path (first non-empty)." },
    { "key": "brightness", "label": "Brightness", "type": "number", "required": false, "default": 0, "min": -100, "max": 100, "help": "Brightness adjustment (-100 to 100). 0 leaves it unchanged." },
    { "key": "contrast", "label": "Contrast", "type": "number", "required": false, "default": 0, "min": -100, "max": 100, "help": "Contrast adjustment (-100 to 100). 0 leaves it unchanged." },
    { "key": "saturation", "label": "Saturation", "type": "number", "required": false, "default": 0, "min": -100, "max": 100, "help": "Saturation adjustment (-100 to 100). 0 leaves it unchanged." },
    { "key": "gamma", "label": "Gamma", "type": "number", "required": false, "default": 1.0, "help": "Gamma correction. >1 brightens, <1 darkens, 1.0 leaves it unchanged." },
    { "key": "sharpen", "label": "Sharpen (sigma px)", "type": "number", "required": false, "default": 0, "help": "Sharpening radius in pixels. 0 disables sharpening." },
    { "key": "blur", "label": "Blur (sigma px)", "type": "number", "required": false, "default": 0, "help": "Gaussian blur sigma in pixels. 0 disables blur." },
    { "key": "grayscale", "label": "Grayscale", "type": "boolean", "required": false, "default": false, "help": "Convert the image to grayscale." },
    { "key": "sepia", "label": "Sepia", "type": "boolean", "required": false, "default": false, "help": "Apply a sepia tone." },
    { "key": "invert", "label": "Invert Colors", "type": "boolean", "required": false, "default": false, "help": "Invert all colors." },
    { "key": "output_field", "label": "Output Field", "type": "text", "required": false, "default": "image_path", "help": "Item field where the output file path is written." },
    { "key": "output_dir", "label": "Output Directory", "type": "text", "required": false, "help": "Directory for the adjusted file. Defaults to the source image's directory. ~ is expanded." }
  ]
}
