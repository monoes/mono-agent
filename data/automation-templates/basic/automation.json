{
  "$schema": "https://raw.githubusercontent.com/monoes/mono-agent/master/data/schemas/automation.v1.schema.json",
  "schema": "monoagent.automation/v1",
  "id": "{{id}}",
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "A starting point: one read-only action and one fragment.",
  "license": "MIT",
  "engine": ">=0.0.0",
  "category": "other",
  "site": {
    "startUrl": "{{startUrl}}",
    "domains": [
      "{{domain}}",
      "*.{{domain}}"
    ]
  },
  "permissions": {
    "steps": [
      "call_fragment",
      "click",
      "extract_text",
      "navigate",
      "wait_for"
    ],
    "scripts": [],
    "downloads": false
  },
  "actions": [
    "get_page_title"
  ],
  "defaults": {
    "humanLike": true,
    "timeoutSec": 60
  },
  "policy": {
    "tier": "standard"
  }
}
