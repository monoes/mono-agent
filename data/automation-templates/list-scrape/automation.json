{
  "$schema": "https://raw.githubusercontent.com/monoes/mono-agent/master/data/schemas/automation.v1.schema.json",
  "schema": "monoagent.automation/v1",
  "id": "{{id}}",
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "Scrape a list of items (title, link, summary) from a page.",
  "license": "MIT",
  "engine": ">=0.0.0",
  "category": "other",
  "site": {
    "startUrl": "{{startUrl}}",
    "domains": [
      "{{domain}}",
      "*.{{domain}}"
    ]
  },
  "permissions": {
    "steps": [
      "extract_table",
      "navigate",
      "save_data",
      "transform",
      "wait_for"
    ],
    "scripts": [],
    "downloads": false
  },
  "actions": [
    "list_items"
  ],
  "defaults": {
    "humanLike": true,
    "timeoutSec": 60
  },
  "policy": {
    "tier": "standard"
  }
}
