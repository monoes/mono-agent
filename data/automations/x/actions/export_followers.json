{
  "$schema": "../../../schemas/action.v1.schema.json",
  "actionType": "export_followers",
  "platform": "X",
  "version": "2.0.0",
  "description": "Export the followers, verified followers or following list of an X (Twitter) profile",
  "sideEffects": "read",
  "metadata": {
    "requiresAuth": true,
    "supportsPagination": true,
    "supportsRetry": true
  },
  "inputs": {
    "required": [
      {
        "name": "target_url",
        "type": "string",
        "description": "Profile URL or username whose followers (or following) list to export",
        "aliases": ["profileUrl", "targetUsername", "targets", "selectedListItems"],
        "ui": { "label": "Profile", "placeholder": "https://x.com/NASA" }
      }
    ],
    "optional": [
      { "name": "sourceType", "type": "select", "default": "FOLLOWERS_FETCH", "options": ["FOLLOWERS_FETCH", "VERIFIED_FOLLOWERS_FETCH", "FOLLOWING_FETCH"], "description": "Which list to export" },
      { "name": "maxResultsCount", "type": "number", "default": 100, "min": 1, "max": 5000, "description": "Maximum users to export" }
    ]
  },
  "outputs": {
    "success": [
      "resultsCount",
      "maxResultsCount"
    ],
    "failure": []
  },
  "steps": [
    {
      "id": "list_followers",
      "type": "call_bot_method",
      "methodName": "list_followers",
      "args": [
        "{{target_url}}",
        "{{sourceType or 'FOLLOWERS_FETCH'}}",
        "{{maxResultsCount or 100}}"
      ],
      "variable_name": "users",
      "timeout": 900,
      "onError": {
        "action": "mark_failed"
      }
    },
    {
      "id": "save_results",
      "type": "save_data",
      "dataSource": "users"
    }
  ],
  "errorHandling": {
    "globalRetries": 1,
    "retryDelay": 2000,
    "onFinalFailure": "log_and_continue"
  }
}
